% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/future_climate.R
\name{get_cmip6_anom}
\alias{get_cmip6_anom}
\title{Download and plot essential climate data}
\usage{
get_cmip6_anom(
  var = "bioc",
  bio = 1,
  ssp = "585",
  period = "2081-2100",
  model = "MPI-ESM1-2-HR",
  res = "10m",
  use_cache = TRUE,
  write_cache = getOption("hs_write_cache")
)
}
\arguments{
\item{var, bio, ssp, period, model, res}{Passed through to \code{\link{get_cmip6}}.}

\item{use_cache}{(boolean) Return cached anomaly data if available, defaults to TRUE.}

\item{write_cache}{(boolean) Write data to cache, defaults to FALSE.}
}
\value{
Invisibly returns a tibble with columns \code{lon}, \code{lat}, and
\code{delta}: the projected change versus the 1970-2000 baseline for each
cell. For temperature variables \code{delta} is the absolute change in
degrees C; for precipitation variables (var "prec", or bioc 12-19) it is
the percent change, capped at +/-100\\% to limit instability where the
baseline is near zero.
}
\description{
Computes expected climate change as the difference between a CMIP6 projection
retrieved from WorldClim and its 1970-2000 reference-period baseline, on a
common one-degree grid.
}
\details{
\code{get_cmip6_anom} downloads the projection and the corresponding
WorldClim v2.1 baseline, aggregates both to a one-degree grid, and subtracts
cell by cell. Differencing removes most of the model's systematic bias,
leaving the expected change. Polar regions can show spurious values where
the downscaled grids differ in land-sea masking.
}
\examples{
\donttest{
anom <- get_cmip6_anom()
plot_cmip6_anom(anom)
}

}
\references{
See \code{\link{get_cmip6}}.
}
\author{
Hernando Cortina, \email{hch@alum.mit.edu}
}
